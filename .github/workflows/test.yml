name: CI Tests

on:
  pull_request:
    branches:
      - main
      - master

jobs:
  test2:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run tests
        run: |
          echo "Running tests..."
          sleep 300
          exit 0
